'use strict';

define([], function () {

    var feature1Provider = function () {

        this.$get = ['$injector', function ($injector) {

        }];

        feature1Provider.$inject = [];
    };

    return feature1Provider;
});